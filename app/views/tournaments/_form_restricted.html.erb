<fieldset>
  <legend>Title</legend>
  <%= f.label :title, "Title" %>
  <%= f.text_field :title %>
</fieldset>
<fieldset>
  <legend>Description</legend>
  <%= f.label :description, "Description (can be blank)" %>
  <%= f.text_area :description %>
</fieldset>
<%= f.submit "Submit" %>
