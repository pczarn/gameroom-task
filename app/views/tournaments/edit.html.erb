<h1>Tournament</h1>

<%= render partial: "tournaments/display", locals: { tournament: @tournament } %>

<%=
  if @tournament.owned_by? current_user
    render partial: "tournaments/display_edit", locals: { tournament: @tournament }
  end
%>
