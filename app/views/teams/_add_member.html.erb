<fieldset>
  <legend>Add a member</legend>
  <% user_ids_names.map do |(id, name)| %>
    <%= radio_button_tag :member_id, id %>
    <%= label_tag :member_id, name, value: id %>
  <% end %>
</fieldset>
<%= hidden_field_tag :id, team.id %>
<%= submit_tag "Submit" %>
